% PLACEHOLDER — run `@paper-writer draft the Abstract` to populate.
% Target structure (high-impact venues, 150–250 words):
%   1-2: scientific problem + why it matters
%     3: what is currently unknown (the gap)
%   4-5: what we did (approach, not method detail)
%   6-7: what we found (1-2 specific results)
%     8: what it means (implication, no overreach)

\textit{[The abstract is a stub. Draft via \texttt{@paper-writer}.]}
