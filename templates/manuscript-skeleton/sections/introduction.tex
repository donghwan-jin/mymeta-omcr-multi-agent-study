% PLACEHOLDER — run `@paper-writer draft the Introduction` to populate.
% Target structure (4–5 paragraphs):
%   1. Hook paragraph — why the big question matters
%   2. State of the field — what is established (cite anchors)
%   3. The gap — what is not known
%   4. Our approach — brief, non-jargon
%   5. Roadmap — what this paper shows
%
% Citation discipline:
%   - Every assertion gets a \citep{citekey}.
%   - Citekeys must already exist in references.bib — request them from
%     @literature-curator by leaving `[CITE: <one-line claim>]` markers
%     that the curator then resolves to verified \citep{...} entries.

\textit{[The Introduction is a stub. Draft via \texttt{@paper-writer}; resolve \texttt{[CITE: ...]} placeholders via \texttt{@literature-curator}.]}
