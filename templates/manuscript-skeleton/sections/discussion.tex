% PLACEHOLDER — run `@paper-writer draft the Discussion` to populate.
% Structure (4–6 paragraphs):
%   1. Summary of key findings (2–3 sentences — not a re-narration of Results)
%   2. Interpretation of main finding
%   3. Relation to prior work (cite competing / closest priors directly)
%   4. Implications for the field
%   5. Limitations (specific, honest, not defensive)
%   6. Conclusion (2–3 sentences carrying the forward-looking implication)

\textit{[The Discussion is a stub. Draft via \texttt{@paper-writer}; framing must be cleared with \texttt{@supervisor} first.]}
