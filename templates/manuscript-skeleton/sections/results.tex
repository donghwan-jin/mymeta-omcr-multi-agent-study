% PLACEHOLDER — run `@paper-writer draft the Results` to populate.
% Structure:
%   - One subsection per main finding, titled with the finding (not "Analysis 1").
%   - Lead with the result, follow with statistics (test, value, effect size, correction).
%   - Reference figures with \ref{fig:label} immediately after the claim they support.
%   - End each subsection with a one-sentence bridge to the next.
%
% Figures: place files in figures/, include like:
%   \begin{figure}[h]
%     \centering
%     \includegraphics[width=\linewidth]{fig1.pdf}
%     \caption{<one-sentence finding statement>}
%     \label{fig:fig1}
%   \end{figure}

\textit{[The Results section is a stub. Draft via \texttt{@paper-writer} once \texttt{@analysis-implementer}'s outputs are in.]}
